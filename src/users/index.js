import * as role from './role';

const users = [
  {
    role: role.ADMIN,
    login: 'admin',
    password: 'admin',
  },
  {
    role: role.USER,
    login: 'user',
    password: 'user',
  },
];

export default users;
